# Admin Configuration
# DEPRECATED: ADMIN_EMAILS is a legacy fallback. Use database-driven RBAC instead.
# To grant admin: UPDATE users SET role = 'ADMIN' WHERE email = 'admin@example.com';
# ADMIN_EMAILS will be removed in a future release.
ADMIN_EMAILS=admin@example.com,superadmin@example.com

# Security: Proxy Trust Configuration
# Set to 'true' ONLY if application is behind a trusted reverse proxy (nginx, Cloudflare, AWS ALB).
# When true, the application will trust the 'x-forwarded-for' header for client IP extraction.
# When false or unset (default), the application will use direct connection IP only.
# WARNING: Setting this to 'true' without a trusted proxy allows IP spoofing attacks.
# Default: false (secure by default)
TRUST_PROXY=false

# Redis Connection Configuration
# REDIS_URL=redis://localhost:6379
# REDIS_MAX_RETRIES_PER_REQUEST=3
# REDIS_RETRY_MAX_ATTEMPTS=10
# REDIS_RETRY_MAX_DELAY_MS=3000

# Account Lockout Configuration (CWE-307 protection)
# Defaults match OWASP recommendations for brute-force prevention.
# LOCKOUT_MAX_ATTEMPTS=5
# LOCKOUT_WINDOW_SECONDS=900
# LOCKOUT_DURATION_FIRST=900
# LOCKOUT_DURATION_SECOND=3600
# LOCKOUT_DURATION_THIRD=21600
# LOCKOUT_DURATION_EXTENDED=86400
