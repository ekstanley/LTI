# LTIP Environment Configuration
# Copy this file to .env and fill in the values

# ============================================================================
# API Configuration
# ============================================================================
NODE_ENV=development
PORT=4000

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ltip_dev

# Redis
REDIS_URL=redis://localhost:6379

# JWT Authentication
# CRITICAL SECURITY: Generate a unique, cryptographically secure secret for JWT token signing
# Minimum length: 32 characters (enforced by Zod validation)
# NEVER commit real secrets to version control
# MUST be unique per environment (dev, staging, production)
#
# Generate a secure secret using one of these methods:
#   openssl rand -base64 32
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   pwgen 64 1  # if pwgen is installed
#
# In production, this MUST be set or the API will fail to start with:
# "FATAL: JWT_SECRET environment variable must be set in production"
#
# Related JWT configuration (optional, defaults shown in config.ts):
#   JWT_ACCESS_TOKEN_EXPIRES_IN=15m
#   JWT_REFRESH_TOKEN_EXPIRES_IN=7d
#   JWT_ISSUER=ltip-api
#   JWT_AUDIENCE=ltip-web
JWT_SECRET=change-this-to-a-real-secret-minimum-32-chars-required

# CORS
CORS_ORIGINS=http://localhost:3000

# Logging
LOG_LEVEL=debug

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# Frontend Configuration
# ============================================================================
NEXT_PUBLIC_API_URL=http://localhost:4000

# ============================================================================
# AI/ML Services (Phase 2)
# ============================================================================
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# ============================================================================
# External APIs
# ============================================================================

# Congress.gov API (PRIMARY) - https://api.congress.gov/
# Register at: https://api.congress.gov/sign-up/
# Note: DEMO_KEY works for testing but has lower rate limits
CONGRESS_API_KEY=

# GovInfo API (BILL TEXT) - https://api.govinfo.gov/
# Register at: https://www.govinfo.gov/developers
# Provides full bill text in XML/PDF/HTML formats
GOVINFO_API_KEY=

# OpenStates API (STATE LEGISLATURE) - https://openstates.org/api/
# Register for free key at: https://openstates.org/accounts/signup/
# OPENSTATES_API_KEY=

# FEC API (CAMPAIGN FINANCE) - https://api.open.fec.gov/
# Note: DEMO_KEY works, no registration required
# FEC_API_KEY=DEMO_KEY

# NewsAPI (NEWS AGGREGATION) - https://newsapi.org/
# Register for free key at: https://newsapi.org/register
# NEWSAPI_KEY=
